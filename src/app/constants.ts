import { VideoSettings } from "@/types";

export const MAX_AVAILABLE_STORAGE_PERCENTAGE = 0.8;

export const DEFAULT_VIDEO_SETTINGS: VideoSettings = {
  width: 1920,
  height: 1080,
  frameRate: 30,
};
